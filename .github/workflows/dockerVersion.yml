name: check docker Version
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: check pwd
        run: |
          pwd >> test.txt
          cat test.txt
