name: check docker apiVersion
on:
  push:
    branches:
      - main
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name: check pwd
        run: |
          pwd >> test.txt
          cat test.txt
